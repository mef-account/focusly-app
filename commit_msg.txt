RBAC migration: profiles.type, project_members, RLS re-enabled

Replace fragile client-side access control with standard Supabase RBAC pattern.

- Add migration 20260618_rbac.sql: profiles.type enum (admin/user), project_members table, is_app_admin() and get_project_role() helpers, drop workspace_members/project_access, re-enable RLS on all tables
- Rename global role from viewer to user in profiles.type; project_members.role stays viewer (project-scoped, future-proof)
- Simplify access.ts to fetchUserType() reading profiles.type directly
- Simplify useCurrentUserRole: useIsViewer() now checks type === user
- Remove all getAccessScope/buildProjectFilter logic from useWorkspace, useProjects, usePortfolios, useTasks — RLS returns correct rows
- Replace /api/team/invite and /api/team/access with single /api/team/members (GET, POST, DELETE)
- Rewrite Settings Team tab to project-centric view
- Add isPermissionDenied and friendlyError to errors.ts; wire into all mutations
